eureka:
  client:
    serviceUrl:
      defaultZone: http://10.1.0.3:8761/eureka/
    register-with-eureka: true
  instance:
    prefer-ip-address: true
spring:
  application:
    name: api-gateway
  cloud:
    discovery:
      enabled: true
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true